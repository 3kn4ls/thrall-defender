apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: thrall-backend-data
  namespace: thrall-defender
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path  # k3s default storage class
  resources:
    requests:
      storage: 5Gi
